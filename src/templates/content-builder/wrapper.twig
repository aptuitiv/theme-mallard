{% import 'macros/macros' as macros %}
<div class="Wrapper Wrapper--bg{{ background|pascal}}{{ macros.blockMargin(margin) }}">
    <div class="Container{{ macros.blockWidth(width) }} first-child-mt-0 last-child-mb-0">
        {% for element in content.elements %}
            {{ element.content }}
        {% endfor %}
    </div>
</div>
