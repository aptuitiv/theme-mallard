<div class="Wrapper Wrapper--bg{{ background|pascal}}{% if topMargin == 'none' %} mt-0{% endif %}">
    <div class="Container {{ width == 'Narrow' ? 'Container--narrow' : 'Container--pad' }} first-child-mt-0 last-child-mb-0">
        {% for element in content.elements %}
            {{ element.content }}
        {% endfor %}
    </div>
</div>
