{%  include 'snippets/header' %}
{% block banner %}
    {{ _page.content('banner') }}
{% endblock %}
<div class="Container">
    <div class="Content clearfix">
        {% if _core.theme.settings.showBreadcrumb == "yes" and _page.isHome == false %}
            {{ _page.breadcrumb() }}
        {% endif %}
        <div class="grid">
            <div class="col-1-1 col-2-3-md push-1-6-md">{% block main %}<main id="main">{{ _page.content() }}</main>{% endblock %}</div>
        </div>
    </div>
</div>
{%  include 'snippets/footer' %}
